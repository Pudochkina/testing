import { ProfilePage } from './ProfilePage';

/**
 * DemoQA uses a profile-based book collection instead of a shopping cart.
 * CartPage aliases ProfilePage to match the test case domain language.
 */
export class CartPage extends ProfilePage {}
